{
  "name": "cloudflare-containers-learning-app",
  "private": true,
  "packageManager": "pnpm@11.5.0",
  "scripts": {
    "dev": "concurrently \"pnpm dev:web\" \"pnpm dev:demos\"",
    "dev:web": "pnpm --filter web dev",
    "dev:demos": "pnpm --filter demos dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md,yml,yaml,css}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md,yml,yaml,css}\"",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "typegen": "pnpm -r typegen",
    "check": "pnpm lint && pnpm format:check && pnpm typecheck && pnpm test && pnpm build",
    "preview:full": "pnpm --filter web exec opennextjs-cloudflare build && wrangler dev -c apps/web/wrangler.jsonc -c workers/demos/wrangler.jsonc",
    "deploy": "pnpm --filter demos deploy && pnpm --filter web deploy"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "prettier": "^3.5.2",
    "wrangler": "^4.86.0"
  }
}
