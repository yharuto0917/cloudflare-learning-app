Cloudflare の開発者プラットフォームは、グローバルエッジネットワーク上で超高速にコードを実行するために設計されています。

## V8 Isolate vs VM/Container

従来のサーバーレス（AWS Lambdaなど）やコンテナ（Docker）と異なり、Cloudflare Workers は V8 Isolate 技術を使用しています。

```ts
// 例: シンプルな Worker の fetch ハンドラー
export default {
  async fetch(request, env, ctx) {
    return new Response("Hello, Cloudflare!");
  },
};
```

<Callout type="tip" title="なぜ速いのか">
  V8 Isolate はプロセスや VM より遥かに軽量なため、コールドスタートがほぼゼロになります。これが
  エッジでの動的実行を現実的にしている要です。
</Callout>

## 主要プロダクトの全体像

Cloudflare の開発者プラットフォームは、Workers を土台にした複数のプリミティブで構成されます。本コースで扱う代表的なものは次の通りです。

- **Workers** — エッジで動的にコードを実行する基盤
- **KV** — 低レイテンシで読める結果整合の Key-Value ストア
- **R2** — **egress（転送）課金が無料**の S3 互換オブジェクトストレージ
- **D1** — SQLite ベースのリレーショナルデータベース
- **Durable Objects** — 強整合な状態を持つ実行単位
- **Containers** — Docker イメージをエッジで実行する計算レイヤー

これらは個別のサービスでありながら、すべて同じ Workers ランタイムから binding 経由で透過的に利用できる点が特徴です。

## 理解度チェック

<Quiz
  questions={[
    {
      question: "Cloudflare Workers が分離に使用している技術は？",
      options: ["コンテナ", "V8 Isolate", "仮想マシン (VM)", "WebAssembly のみ"],
      answer: 1,
      explanation: "Workers は V8 Isolate を使い、軽量で高速な起動(ほぼゼロのコールドスタート)を実現します。",
    },
    {
      question: "オブジェクトストレージ R2 の大きな特徴は？",
      options: ["egress(転送)課金が高い", "egress が無料", "SQL 専用ストア", "メモリ上のみで永続化しない"],
      answer: 1,
      explanation: "R2 は egress 無料の S3 互換オブジェクトストレージで、転送量の多い用途に向きます。",
    },
  ]}
/>

<DocLinks
  links={[
    {
      href: "https://developers.cloudflare.com/workers/",
      title: "Cloudflare Workers ドキュメント",
      description: "Workers の概要と API リファレンス",
    },
    {
      href: "https://developers.cloudflare.com/workers/reference/how-workers-works/",
      title: "How Workers works",
      description: "V8 isolate アーキテクチャの解説",
    },
  ]}
/>
